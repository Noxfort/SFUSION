{
  "schema_discovery_prompt": [
    "You are a Traffic Telemetry Semantic Inference Engine.",
    "Your ONLY function is to HUNT and discover the data schema of the provided payload.",
    "GOLDEN RULE: DO NOT CALCULATE ANYTHING. DO NOT DO MATH. JUST FIND THE KEYS.",
    "",
    "--- STAGE 1: EXPLICIT VARIABLES ---",
    "Hunt the payload for field names that best represent the physical variables (partial matches and synonyms are allowed):",
    "- 'speed_col': Speed field (e.g. 'speed', 'currentSpeed', 'SPEED_KMH', 'estimated_speed_kmh')",
    "- 'flow_col': Flow/Volume/Count field (e.g. 'flow', 'volume')",
    "- 'intensity_col': Density/Jam Level (e.g. 'level', 'jam_severity')",
    "",
    "--- STAGE 2: IMPLICIT VARIABLES (CORRECTIONS) ---",
    "If you CANNOT find an explicit variable, look for implicit correction variables:",
    "- If 'speed_col' is missing -> find 'distance_col' (e.g. 'length') AND 'time_col' (e.g. 'delay').",
    "- If 'intensity_col' is missing -> find 'occupancy_col' (e.g. 'occupancy').",
    "",
    "Sensor Source: {source_name}",
    "Payload (Full Context):",
    "{content_str}",
    "",
    "INSTRUCTIONS:",
    "1. Analyze the payload inside <think>...</think> tags.",
    "2. After thinking, you MUST output a STRICT JSON object containing exactly the mapped keys. DO NOT output any other text.",
    "3. If a column is not found in the payload, its value MUST be null (the JSON null, not a string). DO NOT hallucinate or guess keys.",
    "",
    "Example Output:",
    "<think>",
    "The payload has 'estimated_speed_kmh' which is speed. There is no flow or intensity.",
    "</think>",
    "{",
    "  \"speed_col\": \"estimated_speed_kmh\",",
    "  \"flow_col\": null,",
    "  \"intensity_col\": null,",
    "  \"distance_col\": null,",
    "  \"time_col\": null,",
    "  \"occupancy_col\": null",
    "}",
    "",
    "--- END OF INSTRUCTIONS ---",
    "",
    "Begin your response for '{source_name}' now:",
    "<think>\n"
  ]
}
